<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.antrianrumahsakit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntrianRumahSakit"
        tools:targetApi="31">

        <!-- Splash launcher buat opening app -->
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Role Selection -->
        <activity
            android:name=".ui.RoleSelectionActivity"
            android:exported="true" />

        <!-- Activity untuk masing-masing role -->
        <activity
            android:name=".ui.pasien.MainActivityPasien"
            android:exported="true" />
        <activity
            android:name=".ui.dokter.MainActivityDokter"
            android:exported="true" />
        <activity
            android:name=".ui.admin.MainActivityAdmin"
            android:exported="true" />

    </application>

</manifest>
